<input
    class="form-control {{ $classes ?? '' }}"
    type="text"
    id="{{ $name ?? '' }}"
    name="{{ $name ?? '' }}"
    placeholder="{{ $placeholder ?? '' }}"
    value="{{ $value ?? '' }}"
>
